# Required — PostgreSQL connection string
# Get a free DB at: https://neon.tech  or  https://supabase.com
DATABASE_URL=postgresql://user:password@host:5432/subgen

# Optional — only needed if running the API server standalone (not on Vercel)
PORT=8080
